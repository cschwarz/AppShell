<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AppShell.Samples.Navigation.Mobile.Views.View3">    
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <Label Text="{Binding Title, StringFormat='Mobile View 3: {0}'}" Grid.Row="0" />
    <Button Command="{Binding CloseCommand}" Text="Close" Grid.Row="1" />
  </Grid>
</ContentView>