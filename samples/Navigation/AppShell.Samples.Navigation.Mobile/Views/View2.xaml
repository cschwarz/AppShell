<?xml version="1.0" encoding="utf-8" ?>
<views:ShellView xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:views="clr-namespace:AppShell.Mobile.Views;assembly=AppShell.Mobile"
             x:Class="AppShell.Samples.Navigation.Mobile.Views.View2">  
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <Label Text="{Binding Title, StringFormat='Mobile View 2: {0}'}" Grid.Row="0" />
    <Button Command="{Binding OpenMasterDetailViewModelCommand}" Text="Open" Grid.Row="1" />
  </Grid>
</views:ShellView>