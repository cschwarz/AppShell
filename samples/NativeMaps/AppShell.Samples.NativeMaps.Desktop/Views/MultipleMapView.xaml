<UserControl x:Class="AppShell.Samples.NativeMaps.Desktop.Views.MultipleMapView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AppShell.Samples.NativeMaps.Desktop.Views"
             xmlns:maps="clr-namespace:AppShell.NativeMaps.Desktop;assembly=AppShell.NativeMaps.Desktop"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <maps:MapView Grid.Row="0" Center="{Binding Center1}" ZoomLevel="{Binding ZoomLevel1}" />
        <maps:MapView Grid.Row="1" Center="{Binding Center2}" ZoomLevel="{Binding ZoomLevel2}" />
    </Grid>
</UserControl>
