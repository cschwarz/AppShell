<UserControl x:Class="AppShell.Samples.Navigation.Desktop.Views.View5"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AppShell.Samples.Navigation.Desktop.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding Title, StringFormat='Desktop View 5: {0}'}" Grid.Row="0" />
        <Button Command="{Binding CloseCommand}" Content="Close" Grid.Row="1" />
    </Grid>
</UserControl>
